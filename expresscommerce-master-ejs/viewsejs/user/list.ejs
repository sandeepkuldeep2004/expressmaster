<%- include('../layouts/header') %>
<%if (usergroupList) { %>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title float-right">

                <a href="/register/register" class="btn-floating btn btn-primary btn-sm">
                    <i class="fas fa-plus"></i> Add new user
                </a>
                
               
            </h5>
            <%if(locals.errorMessage) { %>
                    <div class="alert alert-success" role="alert">
                        <%=errorMessage%>
                        </div>
                <% } %> 
            <table class="table table-hover" id="example">
                <thead>
                    <tr>
                        <th scope="col">Name</th>
                        <th scope="col">isSuperAdmin</th>
                        <th scope="col">Base Site</th>
                        <th scope="col">User group</th>
                        <th scope="col">Access Module</th>
                        <th scope="col" class="text-right">Action</th>
                    </tr>
                </thead>
                <tbody>

                    <% usergroupList.forEach(usergroupList => { %> 
                        <tr>
                            <td><span class="dash-status"></span><%=usergroupList.name%><span></td>
                            <td><span class="dash-status"></span><%=usergroupList.issuperadmin%></span></td>
                            <td><span class="dash-status"></span><%=usergroupList.baseSiteDetail%></span></td>
                            <td><span class="dash-status"></span><%=usergroupList.usergroup%></span></td>
                            <td><span class="dash-status"></span><%=usergroupList.accessmoules%></span></td>
                            <td>
                                <div class="bo-edit-row">
                                    <a href="/user/<%=usergroupList._id %>" class="btn btn-float">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form action="/user/remove/<%=usergroupList._id %>" method="post" id="delete-form">
                                        <input type="hidden" name="_csrf" value="<%=csrfToken %>">
                                        <button type="submit" class="btn red">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                               
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
        <%} else{ %>
            <p>Not found</p>
            <% } %>
    </div>
    </div>
    <%- include('../layouts/footer') %>

 