<%- include('../layouts/header') %>
<%if (userCustomerList) { %>
    <div class="card">
        <div class="card-body">
            <h5 class="card-title float-right">

                <a href="/register/register" class="btn-floating btn btn-primary btn-sm">
                    <i class="fas fa-plus"></i> Add new Customer
                </a>
                
               
            </h5>
            <%if(locals.errorMessage) { %>
                    <div class="alert alert-success" role="alert">
                        <%=errorMessage%>
                        </div>
                <% } %> 
            <table class="table table-hover" id="example">
                <thead>
                    <tr>
                        <th scope="col">UId</th>
                        <th scope="col">Name</th>
                        <th scope="col">Email</th>
                        <th scope="col">Creation Date</th>

                        
                        <!--th scope="col">User group</th>
                        <th scope="col">Access Module</th>-->
                        <th scope="col" class="text-right">Action</th>
                    </tr>
                </thead>
                <tbody>

                    <% userCustomerList.forEach(userCustomerList => { %> 
                        <tr>
                            <td><span class="dash-status"></span><%=userCustomerList.uid%><span></td>

                            <td><span class="dash-status"></span><%=userCustomerList.name%><span></td>
                            <td><span class="dash-status"></span><%=userCustomerList.email%></span></td>
                          <td><span class="dash-status"></span><%=moment(userCustomerList.creationDate).format('Do MMMM, YYYY');%>                            
                            </span></td>
                            <!-- <td><span class="dash-status"></span><%=userCustomerList.usergroup%></span></td>
                            <td><span class="dash-status"></span><%=userCustomerList.accessmoules%></span></td>
                          --> <td>
                                <div class="bo-edit-row">
                                    <a href="/user/<%=userCustomerList._id %>" class="btn btn-float">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form action="/user/remove/<%=userCustomerList._id %>" method="post" id="delete-form">
                                        <input type="hidden" name="_csrf" value="<%=csrfToken %>">
                                        <button type="submit" class="btn red">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                               
                            </td>
                        </tr>
                        <% }) %>
                </tbody>
            </table>
        </div>
        <%} else{ %>
            <p>Not found</p>
            <% } %>
    </div>
    </div>
    <%- include('../layouts/footer') %>

 