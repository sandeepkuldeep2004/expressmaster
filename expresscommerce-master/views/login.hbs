<h4><i class="fas fa-shopping-cart"></i> Xpress Commerce</h4>
<div class="section">
    <p class="lead">Your commerce journey is just a click way</p>
</div>
<div class="divider"></div>
<div class="section">
    <form action="/users/login" method="POST">
     <input type="hidden" name="_csrf" value="{{csrfToken}}">
          <div class="form-group">
            <label for="email">Email</label>
            <input
              type="email"
              id="email"
              name="email"
              class="form-control"
              placeholder="Enter Email"
            />
          </div>
          <div class="form-group">
            <label for="password">Password</label>
            <input
              type="password"
              id="password"
              name="password"
              class="form-control"
              placeholder="Enter Password"
            />
          </div>
          <button type="submit" class="btn btn-primary btn-block">Login</button>
          <p class="lead mt-4">Don't have account ? <a href="/users/register">Register</a></p>
        </form>
</div>